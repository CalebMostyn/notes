Automatic torrent indexing server. Currently just indexing 1337x. To get around cloudflare restrictions and authentication, using [[Byparr]] as an indexer proxy. Link to [dashboard](http://192.168.4.93:9696/).

Connects to apps through Prowlarr setup, not other way around (`Settings -> Apps`).